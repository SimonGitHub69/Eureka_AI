from django.apps import AppConfig


class VettoriConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.vettori"
    label = "vettori"
    verbose_name = "Spedizionieri"
